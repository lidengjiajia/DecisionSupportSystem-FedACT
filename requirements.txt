# ===========================================
# FedACT - 联邦学习对抗攻击防御系统依赖
# ===========================================

# ============ 深度学习框架 ============
torch>=2.0.0
torchvision>=0.15.0

# ============ 科学计算 ============
numpy>=1.24.0
scipy>=1.10.0

# ============ 机器学习 ============
scikit-learn>=1.2.0

# ============ 数据处理 ============
pandas>=2.0.0
h5py>=3.0.0
openpyxl>=3.1.0

# ============ 可视化 ============
matplotlib>=3.7.0
seaborn>=0.12.0

# ============ 进度条 ============
tqdm>=4.65.0

# ============ 不平衡学习 ============
imbalanced-learn>=0.10.0

# ===========================================
# 安装说明
# ===========================================
# 1. 创建conda环境:
#    conda create -n fedact python=3.10
#    conda activate fedact
#
# 2. 安装PyTorch (RTX 4090使用CUDA 12.x):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# 3. 安装其他依赖:
#    pip install -r requirements.txt
#
# 4. 验证GPU:
#    python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}, GPUs: {torch.cuda.device_count()}')"
#
# ===========================================
# GPU并行实验说明
# ===========================================
# - 实验框架使用 ProcessPoolExecutor + spawn 模式
# - 确保每个子进程获得独立的 CUDA 上下文
# - 通过 CUDA_VISIBLE_DEVICES 环境变量分配GPU
# - 默认每块GPU并发3个实验 (在 run_ablation.py 中配置)
#
# 测试GPU并行:
#    python test_gpu_parallel.py
#
# 运行实验:
#    python run_ablation.py
